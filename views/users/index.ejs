<%- include('../partials/header') %>
<h1 style="margin-top:2%">Users</h1>
  <p class="userstext">This webpage showcases a demonstration created with Node.js/Express.js and EJS templates.<br>It illustrates a design pattern for listing and viewing content, fetching information from a JSON file.<br>The images and text are produced using third-party APIs and do not involve real individuals or identities.</p>
 <table class="table table-hover table-striped">
	<thead>
   	<tr>
      	<th scope="col">First Name</th>
      	<th scope="col">Last Name</th>
      	<th scope="col">Email</th>
   	</tr>
	</thead>
 <tbody>

<% users.forEach(function(user){ %>
  <tr>
   	<td><a href="users/view/<%= user.id %>"><%= user.first_name %></a></td>
   	<td><%= user.last_name %></td>
   	<td><%= user.email %></td>
  </tr>
<% }); %>

</tbody>
</table>

<%- include('../partials/footer') %>
